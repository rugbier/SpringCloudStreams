server:
  port: 8081
spring:
  cloud:
    stream:
      default-binder: kafka
      kafka:
        binder:
          headers: email, order
          brokers:
            - localhost:9092
      bindings:
        email:
          binder: kafka
          destination: emailQueue
          content-type: text/plain
          group: emailQueue-group